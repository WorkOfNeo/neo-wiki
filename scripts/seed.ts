/**
 * Intentionally empty.
 *
 * The previous version of this script seeded 8 fabricated example entries to
 * give the graph nodes to render against during development. Those seeds were
 * confidently-wrong placeholders (e.g. inventing a Mikenta Webflow → Monday
 * sync flow that doesn't exist) and they polluted semantic search.
 *
 * Real entries are now written via the save-to-wiki skill from actual sessions
 * with the truthfulness rules enforced. Don't re-introduce a placeholder seed
 * unless every entry describes work that genuinely happened — fabricated
 * "representative" data is worse than an empty wiki.
 *
 * If you do need to bulk-import, write a one-off script that reads from a
 * markdown directory of real notes, never invent content.
 *
 *   npm run seed   # no-op
 */
console.log("scripts/seed.ts is intentionally empty — see file header.");
